const initialState = {
	contacts: null,
	currentContact: null,
	filteredContacts: null,
	loading: false,
	error: null
};

let actionState = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default actionState